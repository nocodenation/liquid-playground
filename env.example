# =============================================================================
# Liquid Playground Environment Configuration
# =============================================================================
# Copy this file to .env and modify the values as needed.
# All variables are optional - if not set, default values will be used.
# =============================================================================

# =============================================================================
# BUILD CONFIGURATION (used by build.sh)
# =============================================================================

# SYSTEM_DEPENDENCIES
# -------------------
# A comma-separated list of apt packages to install in the Docker image.
# These are system libraries that might be required for certain Python packages
# to work properly (not Python packages themselves - those are managed by NiFi).
#
# Example: Install Tesseract OCR and related libraries
# SYSTEM_DEPENDENCIES="tesseract-ocr, tesseract-ocr-eng, libtesseract-dev, libleptonica-dev, pkg-config"
#
# Example: Install multiple system packages
# SYSTEM_DEPENDENCIES="poppler-utils, libgl1, ffmpeg"
#
# Default: empty (no additional packages)
SYSTEM_DEPENDENCIES=""

# POST_INSTALLATION_COMMANDS
# --------------------------
# A comma-separated list of shell commands to run after package installation.
# Useful for installing additional tools or configuring the environment.
#
# Example: Install Playwright dependencies
# POST_INSTALLATION_COMMANDS="playwright install-deps"
#
# Example: Install Bun runtime
# POST_INSTALLATION_COMMANDS="curl -fsSL https://bun.sh/install | bash, cp /root/.bun/bin/bun /usr/local/bin/bun, chmod +x /usr/local/bin/bun"
#
# Example: Multiple commands
# POST_INSTALLATION_COMMANDS="playwright install-deps, ls -la /"
#
# Default: empty (no post-installation commands)
POST_INSTALLATION_COMMANDS=""

# PERSIST_NIFI_STATE
# ------------------
# Whether to persist NiFi state (flows, configuration, repositories) across restarts.
# When set to "true":
#   - build.sh will create a ./state folder and copy default NiFi directories from the image
#   - start.sh will mount the ./state folder to preserve data between container restarts
#
# The following directories are persisted:
#   - conf (NiFi configuration)
#   - database_repository (NiFi database)
#   - flowfile_repository (FlowFile data)
#   - content_repository (Content data)
#   - provenance_repository (Provenance data)
#
# If ./state folder already exists when running build.sh, you will be prompted
# to confirm whether to overwrite its contents.
#
# Values: "true" or "false"
# Default: "false"
PERSIST_NIFI_STATE="false"

# ENVIRONMENT_VARIABLES
# ---------------------
# A comma-separated list of environment variables to pass into the container.
# Format: "VAR_NAME=value" for each variable.
# These variables will be available inside the running container.
#
# Example: Pass a single environment variable
# ENVIRONMENT_VARIABLES="MY_API_KEY=abc123"
#
# Example: Pass multiple environment variables
# ENVIRONMENT_VARIABLES="MY_API_KEY=abc123, DEBUG=true, LOG_LEVEL=info"
#
# Example: Pass database connection settings
# ENVIRONMENT_VARIABLES="DB_HOST=localhost, DB_PORT=5432, DB_NAME=mydb"
#
# Default: empty (no additional environment variables)
ENVIRONMENT_VARIABLES=""

# =============================================================================
# START CONFIGURATION (used by start.sh)
# =============================================================================

# NIFI_USERNAME
# -------------
# The username for NiFi authentication.
# Must be provided together with NIFI_PASSWORD to take effect.
# If not set, NiFi will generate a random username on startup.
#
# Example:
# NIFI_USERNAME="admin"
#
# Default: empty (NiFi generates random username)
NIFI_USERNAME=""

# NIFI_PASSWORD
# -------------
# The password for NiFi authentication.
# Must be at least 12 characters long.
# Must be provided together with NIFI_USERNAME to take effect.
# If not set, NiFi will generate a random password on startup.
#
# Example:
# NIFI_PASSWORD="mysecurepassword123"
#
# Default: empty (NiFi generates random password)
NIFI_PASSWORD=""

# ADDITIONAL_EXTENSION_PATHS
# ---------------
# A comma-separated list of paths to Python processors or NAR files to mount.
# The script automatically detects the file type:
#   - Python files (.py) → mounted to Python extensions
#   - NAR files (.nar) → mounted to NiFi lib
#   - Folders with Python files → mounted as Python extension folders
#   - Folders with NAR files → each NAR file is mounted individually
#
# Example: Mount a single processor directory
# ADDITIONAL_EXTENSION_PATHS="./example/ParseDocument"
#
# Example: Mount multiple extensions
# ADDITIONAL_EXTENSION_PATHS="/path/to/python_processor, /path/to/my-extension.nar, ./my_processors"
#
# Example: Mount specific files
# ADDITIONAL_EXTENSION_PATHS="/home/user/MyProcessor.py, /home/user/my-service.nar"
#
# Default: empty (no additional extensions mounted)
ADDITIONAL_EXTENSION_PATHS=""

# ADDITIONAL_PORT_MAPPINGS
# ------------------------
# A comma-separated list of additional port mappings to expose from the container.
# Format: "host_port:container_port" for each mapping.
# Port 8443 is always mapped by default for the NiFi web UI.
#
# Example: Expose a single additional port
# ADDITIONAL_PORT_MAPPINGS="8999:8999"
#
# Example: Expose multiple ports (for OAuth callbacks, custom HTTP endpoints, etc.)
# ADDITIONAL_PORT_MAPPINGS="8999:8999, 5432:5432, 1337:1337"
#
# Example: Expose ports for Node.js apps and API gateway
# ADDITIONAL_PORT_MAPPINGS="8888:8888, 3000:3000, 5050:5050"
#
# Default: empty (only port 8443 is mapped)
ADDITIONAL_PORT_MAPPINGS=""
